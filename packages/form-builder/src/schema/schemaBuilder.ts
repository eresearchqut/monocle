import {V1Form} from '@trrf/form-definition'
import {JsonSchema} from "@jsonforms/core";

export const buildSchema = (form: V1Form | undefined): JsonSchema | undefined =>  {
    if (form) {

    }
    return  undefined;
};



