{
    "name": "@trrf/form-definition",
    "version": "1.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "license": "MIT",
    "repository": {
        "type": "git",
        "directory": "packages/form-definition",
        "url": "https://github.com/eresearchqut/future-state-mono-repo.git"
    },
    "scripts": {
        "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
        "prepack": "yarn build",
        "schema": "quicktype --src src/interfaces/form.ts --lang json-schema --out src/interfaces/form.json",
        "build": "yarn clean && tsc --build && cp \"./package.json\" ./dist/",
        "test": "echo 'notest'",
        "lint": "eslint \"./src/**/*.{ts,tsx}\" --max-warnings=0"
    },
    "devDependencies": {
        "quicktype": "^15.0.260"
    },
    "jest": {
        "testEnvironment": "jsdom",
        "moduleFileExtensions": [
            "ts",
            "tsx",
            "js"
        ],
        "transform": {
            "^.+\\.(ts|tsx)$": "ts-jest"
        },
        "testMatch": [
            "**/test/**/*.test.ts",
            "**/test/**/*.test.tsx"
        ],
        "testPathIgnorePatterns": [
            "/node_modules/",
            "/dist/"
        ]
    }
}
