import React, { FunctionComponent } from 'react';

export interface UserAttributeProps {}

export const UserAttributes: FunctionComponent<UserAttributeProps> = (props) => {
    return <div className={'user-attributes'}></div>;
};

export default UserAttributes;
